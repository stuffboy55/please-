cat > .upsun/config.yaml << 'EOF'
applications:
  app:
    type: 'nodejs:20'
    source:
      root: '/'

routes:
  'https://{default}/':
    type: proxy
    upstream: 'http://178.62.247.103:80'
  'http://{default}/':
    type: redirect
    to: 'https://{default}/'
EOF
