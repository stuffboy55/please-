cat > .upsun/config.yaml << 'EOF'
applications:
    proxy-only:
        type: "nodejs:20"

routes:
    "https://{default}/":
        type: proxy
        to: "http://178.62.247.103:80"
    "http://{default}/":
        type: redirect
        to: "https://{default}/"
EOF
